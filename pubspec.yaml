name: code_coverage
description: A CLI app that displays code coverage of a given package
version: 1.0.0-SNAPSHOT
repository: https://github.com/gustavo-hillesheim/dart-code-coverage

environment:
    sdk: ">=2.12.0 <3.0.0"

executables:
    code_coverage:

dependencies:
    path: ^1.7.0
    args: ^2.0.0
    coverage: ^1.0.2
    ansicolor: ^2.0.1
    yaml: ^3.1.0

dev_dependencies:
    test: ^1.16.8
    pedantic: ^1.9.0
